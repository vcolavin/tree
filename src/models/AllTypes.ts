// TODO: This seems a little smelly
// is there a way to compose this list somehow?

// TODO: to cut down on save file size, this could be a regular enum
// rather than a string enum

/** Registry of all types of things */
export enum AllTypes {
  person = "person",
  tree = "tree",
  limb = "limb",
  leaf = "leaf",
}
